---
title: "Function wrap_finalizer_fn"
sidebar-title: "wrap_finalizer_fn"
description: "Wrap a C finalizer callback into a `Box<dyn FnOnce() -> Json + Send>` for use by the core runtime. The callback is invoked exactly once when the stream is exhausted. The returned C string is parsed as JSON and then freed."
position: 4
---
{/* SPDX-FileCopyrightText: Copyright (c) 2026, NVIDIA CORPORATION & AFFILIATES. All rights reserved.
SPDX-License-Identifier: Apache-2.0 */}

Generated from `cargo doc --no-deps -p nemo-relay -p nemo-relay-adaptive -p nemo-relay-pii-redaction -p nemo-relay-ffi`.

<pre className="rust-signature"><code dangerouslySetInnerHTML={{"__html": "pub fn wrap_finalizer_fn(\n    cb: <a href=\"type-nemorelayfinalizercb\">NemoRelayFinalizerCb</a>,\n) -&gt; <a href=\"https://doc.rust-lang.org/1.93.0/alloc/boxed/struct.Box.html\">Box</a>&lt;dyn <a href=\"https://doc.rust-lang.org/1.93.0/core/ops/function/trait.FnOnce.html\">FnOnce</a>() -&gt; <a href=\"https://docs.rs/serde_json/1.0.149/serde_json/value/enum.Value.html\">Value</a> + <a href=\"https://doc.rust-lang.org/1.93.0/core/marker/trait.Send.html\">Send</a>&gt;"}} /></pre>

Wrap a C finalizer callback into a `Box<dyn FnOnce() -> Json + Send>` for use by the core runtime. The callback is invoked exactly once when the stream is exhausted. The returned C string is parsed as JSON and then freed.

## Safety

The caller must ensure `cb` remains valid until the returned closure is invoked. The C callback must return a valid, heap-allocated JSON C string (or null, in which case `Json::Null` is returned).
